# وصف إعادة بناء تطبيق صبر إلكترونكس

## الهدف

إعادة بناء تطبيق جوال محلي لمحل **صبر إلكترونكس** باستخدام Flutter وDart وواجهة عربية RTL، بحيث يعمل على Android وiPhone من دون خادم أو حساب مستخدم أو مفتاح API. يجب أن تكون بيانات المحل والصور والإعدادات محفوظة محلياً على الهاتف، مع توفير نسخ احتياطي واستعادة يدوية.

## الهوية وتجربة الاستخدام

يستخدم التطبيق اسم «صبر إلكترونكس» في العنوان والصفحات، مع هوية لونية تعتمد الكحلي والأخضر التركوازي والبرتقالي. جميع النصوص عربية، واتجاه الواجهة RTL، مع تصميم Material 3 واضح ومناسب للاستخدام اليومي في محل صيانة. يجب أن تكون الأزرار كبيرة، والنماذج قصيرة، وحالات الأجهزة ظاهرة بألوان مميزة.

## الوظائف الأساسية المطلوبة

1. **لوحة التحكم:** عرض الأجهزة قيد المتابعة، الجاهزة للتسليم، المتأخرة، وعدد الأجهزة والإجمالي المدفوع.
2. **سجل الأجهزة:** إضافة جهاز جديد وتعديله وأرشفته، مع رقم طلب تلقائي وبيانات الزبون ورقم الهاتف ونوع الجهاز والموديل.
3. **حالات الإصلاح:** تم الاستلام، قيد الفحص، بانتظار موافقة الزبون، قيد الإصلاح، جاهز للتسليم، تم التسليم، لم يتم الإصلاح، وملغي.
4. **التواريخ والمبالغ:** تاريخ الاستلام، الموعد المتوقع، مبلغ الإصلاح، المبلغ المدفوع، المتبقي، والملاحظات.
5. **البحث والتصفية:** البحث بالاسم ورقم الهاتف ورقم الطلب ونوع الجهاز والموديل، مع تصفية حسب الحالة.
6. **تفاصيل الجهاز:** عرض السجل الكامل، تغيير الحالة، إرسال رسالة WhatsApp جاهزة، مشاركة السجل، وأرشفة الجهاز.
7. **الصور:** التقاط صورة بالكاميرا أو اختيار صور متعددة من المعرض، وحفظ مسارات الصور محلياً مع السجل.
8. **التقارير:** تقارير الحالات والمبالغ وأنواع الأجهزة، وتصدير CSV متوافق مع Excel وPDF عند توفر حزم التصدير.
9. **النسخ الاحتياطي:** تصدير كل البيانات والإعدادات والصور أو مراجعها إلى JSON، واختيار ملف JSON لاستعادته محلياً.
10. **التنبيهات:** تنبيهات محلية للأجهزة الجاهزة للتسليم والمتأخرة.
11. **الحماية:** قفل اختياري برمز PIN أو البصمة/Face ID باستخدام التخزين الآمن وواجهات المصادقة الأصلية.
12. **الخصوصية:** لا تُرسل بيانات الزبائن إلى الإنترنت. يجب إظهار تنبيه للمستخدم بأن حذف التطبيق أو فقدان الهاتف قد يؤدي إلى فقدان البيانات ما لم ينشئ نسخة احتياطية.

## التخزين والبنية التقنية

- Flutter stable وDart وTypeScript غير مطلوب؛ لغة التطبيق هي Dart.
- تخزين السجلات والإعدادات عبر `shared_preferences` أو قاعدة SQLite محلية عند توسع البيانات.
- استخدام `image_picker` للكاميرا والمعرض، و`path_provider` لمسارات الملفات.
- استخدام `url_launcher` لفتح WhatsApp، و`share_plus` للمشاركة، و`file_picker` للاستعادة.
- فصل طبقة النماذج والتخزين عن الشاشات عند تطوير النسخة الكاملة.
- إضافة اختبارات للنماذج، التحويل إلى JSON، البحث، وحساب الإحصاءات.

## البناء والتوزيع

ينبغي أن يحتوي المستودع على workflow في `.github/workflows/android-release.yml` يقوم بالخطوات التالية:

1. تثبيت Flutter stable.
2. توليد مجلد Android إذا لم يكن موجوداً.
3. تثبيت الحزم.
4. تشغيل `flutter analyze`.
5. بناء `flutter build apk --release`.
6. رفع APK كـ Artifact باسم `sabr-electronics-release-apk`.
7. نشر APK كأصل في GitHub Release عام.

يجب عدم تضمين مفاتيح أو بيانات حساسة في المستودع. نسخة Android العامة قابلة للتثبيت ولا تحتاج إلى تشغيل Metro أو توصيل الهاتف بالكمبيوتر. يتطلب بناء iOS جهاز macOS وحساب Apple Developer.

## معايير القبول

يُعد الإصدار جاهزاً عندما يستطيع المستخدم تشغيل التطبيق، إضافة جهاز، البحث عنه، تغيير حالته، حفظ البيانات بعد إغلاق التطبيق، إنشاء نسخة JSON واستعادتها، مشاركة سجل أو رسالة WhatsApp، ورؤية الإحصاءات. كما يجب نجاح تحليل Dart وبناء APK Release وظهور رابط تنزيل عام من صفحة Releases.

## أوامر التشغيل المحلية

```bash
flutter pub get
flutter run
flutter build apk --release
```

رابط المستودع العام: https://github.com/user1cars-lab/sabr-electronics-flutter
